# kentskooking - ComfyUI Custom Nodes
# Triangle wave-controlled iterative video sampling

# All core dependencies are already provided by ComfyUI's requirements.txt
# This file is provided for reference and documentation purposes.

# Core dependencies (provided by ComfyUI):
torch
numpy>=1.25.0
Pillow
safetensors>=0.4.2

# Optional dependencies:
# The following custom nodes are optional and enable additional features if installed:
# - comfyui_ipadapter_plus: For IPAdapter wave modulation (wave_ipadapter_*.py nodes)
#   Install: cd custom_nodes && git clone https://github.com/cubiq/ComfyUI_IPAdapter_plus.git comfyui_ipadapter_plus
